# Default free model (Gemini Flash) — required for public use
GOOGLE_AI_API_KEY=your-google-ai-key-here

# Optional: Anthropic key for server-side use (not needed if users bring their own)
# ANTHROPIC_API_KEY=your-key-here
